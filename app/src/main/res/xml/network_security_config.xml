<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- VLC's built-in web interface commonly uses plain HTTP on the trusted LAN. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
